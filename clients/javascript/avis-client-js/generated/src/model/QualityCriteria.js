/**
 * avis
 * VUE Autonomous Visual Inspection System (AVIS)
 *
 * The version of the OpenAPI document: 0.8.0
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from "../ApiClient";

/**
 * The QualityCriteria model module.
 * @module model/QualityCriteria
 * @version 0.8.0
 */
class QualityCriteria {
  /**
   * Constructs a new <code>QualityCriteria</code>.
   * @alias module:model/QualityCriteria
   * @param id {Number}
   * @param team {Number}
   * @param goodQualityClasses {Array.<String>}
   * @param uncertainQualityClasses {Array.<String>}
   * @param badQualityClasses {Array.<String>}
   * @param createdAt {Date}
   * @param updatedAt {Date}
   */
  constructor(
    id,
    team,
    goodQualityClasses,
    uncertainQualityClasses,
    badQualityClasses,
    createdAt,
    updatedAt,
  ) {
    QualityCriteria.initialize(
      this,
      id,
      team,
      goodQualityClasses,
      uncertainQualityClasses,
      badQualityClasses,
      createdAt,
      updatedAt,
    );
  }

  /**
   * Initializes the fields of this object.
   * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
   * Only for internal use.
   */
  static initialize(
    obj,
    id,
    team,
    goodQualityClasses,
    uncertainQualityClasses,
    badQualityClasses,
    createdAt,
    updatedAt,
  ) {
    obj["id"] = id;
    obj["team"] = team;
    obj["good_quality_classes"] = goodQualityClasses;
    obj["uncertain_quality_classes"] = uncertainQualityClasses;
    obj["bad_quality_classes"] = badQualityClasses;
    obj["created_at"] = createdAt;
    obj["updated_at"] = updatedAt;
  }

  /**
   * Constructs a <code>QualityCriteria</code> from a plain JavaScript object, optionally creating a new instance.
   * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
   * @param {Object} data The plain JavaScript object bearing properties of interest.
   * @param {module:model/QualityCriteria} obj Optional instance to populate.
   * @return {module:model/QualityCriteria} The populated <code>QualityCriteria</code> instance.
   */
  static constructFromObject(data, obj) {
    if (data) {
      obj = obj || new QualityCriteria();

      if (data.hasOwnProperty("id")) {
        obj["id"] = ApiClient.convertToType(data["id"], "Number");
      }
      if (data.hasOwnProperty("team")) {
        obj["team"] = ApiClient.convertToType(data["team"], "Number");
      }
      if (data.hasOwnProperty("configurations")) {
        obj["configurations"] = ApiClient.convertToType(
          data["configurations"],
          ["Number"],
        );
      }
      if (data.hasOwnProperty("good_quality_classes")) {
        obj["good_quality_classes"] = ApiClient.convertToType(
          data["good_quality_classes"],
          ["String"],
        );
      }
      if (data.hasOwnProperty("uncertain_quality_classes")) {
        obj["uncertain_quality_classes"] = ApiClient.convertToType(
          data["uncertain_quality_classes"],
          ["String"],
        );
      }
      if (data.hasOwnProperty("bad_quality_classes")) {
        obj["bad_quality_classes"] = ApiClient.convertToType(
          data["bad_quality_classes"],
          ["String"],
        );
      }
      if (data.hasOwnProperty("created_at")) {
        obj["created_at"] = ApiClient.convertToType(data["created_at"], "Date");
      }
      if (data.hasOwnProperty("updated_at")) {
        obj["updated_at"] = ApiClient.convertToType(data["updated_at"], "Date");
      }
    }
    return obj;
  }

  /**
   * Validates the JSON data with respect to <code>QualityCriteria</code>.
   * @param {Object} data The plain JavaScript object bearing properties of interest.
   * @return {boolean} to indicate whether the JSON data is valid with respect to <code>QualityCriteria</code>.
   */
  static validateJSON(data) {
    // check to make sure all required properties are present in the JSON string
    for (const property of QualityCriteria.RequiredProperties) {
      if (!data[property]) {
        throw new Error(
          "The required field `" +
            property +
            "` is not found in the JSON data: " +
            JSON.stringify(data),
        );
      }
    }
    // ensure the json data is an array
    if (!Array.isArray(data["configurations"])) {
      throw new Error(
        "Expected the field `configurations` to be an array in the JSON data but got " +
          data["configurations"],
      );
    }
    // ensure the json data is an array
    if (!Array.isArray(data["good_quality_classes"])) {
      throw new Error(
        "Expected the field `good_quality_classes` to be an array in the JSON data but got " +
          data["good_quality_classes"],
      );
    }
    // ensure the json data is an array
    if (!Array.isArray(data["uncertain_quality_classes"])) {
      throw new Error(
        "Expected the field `uncertain_quality_classes` to be an array in the JSON data but got " +
          data["uncertain_quality_classes"],
      );
    }
    // ensure the json data is an array
    if (!Array.isArray(data["bad_quality_classes"])) {
      throw new Error(
        "Expected the field `bad_quality_classes` to be an array in the JSON data but got " +
          data["bad_quality_classes"],
      );
    }

    return true;
  }
}

QualityCriteria.RequiredProperties = [
  "id",
  "team",
  "good_quality_classes",
  "uncertain_quality_classes",
  "bad_quality_classes",
  "created_at",
  "updated_at",
];

/**
 * @member {Number} id
 */
QualityCriteria.prototype["id"] = undefined;

/**
 * @member {Number} team
 */
QualityCriteria.prototype["team"] = undefined;

/**
 * @member {Array.<Number>} configurations
 */
QualityCriteria.prototype["configurations"] = undefined;

/**
 * @member {Array.<String>} good_quality_classes
 */
QualityCriteria.prototype["good_quality_classes"] = undefined;

/**
 * @member {Array.<String>} uncertain_quality_classes
 */
QualityCriteria.prototype["uncertain_quality_classes"] = undefined;

/**
 * @member {Array.<String>} bad_quality_classes
 */
QualityCriteria.prototype["bad_quality_classes"] = undefined;

/**
 * @member {Date} created_at
 */
QualityCriteria.prototype["created_at"] = undefined;

/**
 * @member {Date} updated_at
 */
QualityCriteria.prototype["updated_at"] = undefined;

export default QualityCriteria;
