/**
 * avis
 * VUE Autonomous Visual Inspection System (AVIS)
 *
 * The version of the OpenAPI document: 0.8.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import QualityCriteria from './QualityCriteria';

/**
 * The PaginatedQualityCriteriaList model module.
 * @module model/PaginatedQualityCriteriaList
 * @version 0.8.0
 */
class PaginatedQualityCriteriaList {
    /**
     * Constructs a new <code>PaginatedQualityCriteriaList</code>.
     * @alias module:model/PaginatedQualityCriteriaList
     * @param count {Number} 
     * @param results {Array.<module:model/QualityCriteria>} 
     */
    constructor(count, results) { 
        
        PaginatedQualityCriteriaList.initialize(this, count, results);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, count, results) { 
        obj['count'] = count;
        obj['results'] = results;
    }

    /**
     * Constructs a <code>PaginatedQualityCriteriaList</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/PaginatedQualityCriteriaList} obj Optional instance to populate.
     * @return {module:model/PaginatedQualityCriteriaList} The populated <code>PaginatedQualityCriteriaList</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new PaginatedQualityCriteriaList();

            if (data.hasOwnProperty('count')) {
                obj['count'] = ApiClient.convertToType(data['count'], 'Number');
            }
            if (data.hasOwnProperty('next')) {
                obj['next'] = ApiClient.convertToType(data['next'], 'String');
            }
            if (data.hasOwnProperty('previous')) {
                obj['previous'] = ApiClient.convertToType(data['previous'], 'String');
            }
            if (data.hasOwnProperty('results')) {
                obj['results'] = ApiClient.convertToType(data['results'], [QualityCriteria]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>PaginatedQualityCriteriaList</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>PaginatedQualityCriteriaList</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of PaginatedQualityCriteriaList.RequiredProperties) {
            if (!data[property]) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['next'] && !(typeof data['next'] === 'string' || data['next'] instanceof String)) {
            throw new Error("Expected the field `next` to be a primitive type in the JSON string but got " + data['next']);
        }
        // ensure the json data is a string
        if (data['previous'] && !(typeof data['previous'] === 'string' || data['previous'] instanceof String)) {
            throw new Error("Expected the field `previous` to be a primitive type in the JSON string but got " + data['previous']);
        }
        if (data['results']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['results'])) {
                throw new Error("Expected the field `results` to be an array in the JSON data but got " + data['results']);
            }
            // validate the optional field `results` (array)
            for (const item of data['results']) {
                QualityCriteria.validateJSON(item);
            };
        }

        return true;
    }


}

PaginatedQualityCriteriaList.RequiredProperties = ["count", "results"];

/**
 * @member {Number} count
 */
PaginatedQualityCriteriaList.prototype['count'] = undefined;

/**
 * @member {String} next
 */
PaginatedQualityCriteriaList.prototype['next'] = undefined;

/**
 * @member {String} previous
 */
PaginatedQualityCriteriaList.prototype['previous'] = undefined;

/**
 * @member {Array.<module:model/QualityCriteria>} results
 */
PaginatedQualityCriteriaList.prototype['results'] = undefined;






export default PaginatedQualityCriteriaList;

